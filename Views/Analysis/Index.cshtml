@model IEnumerable<fag_el_gamous.Models.Analysis>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Analysistype)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doneby)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Analysisid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Analysistype)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doneby)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Analysisid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
